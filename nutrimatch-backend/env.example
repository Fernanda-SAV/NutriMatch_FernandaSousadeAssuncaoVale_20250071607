# ===============================
# NutriMatch — Exemplo de variáveis
# ===============================
# Copie este arquivo para .env e preencha os valores:
#   Windows (PowerShell): Copy-Item .env.example .env
#   Windows (CMD):       copy .env.example .env
#   Linux/macOS:         cp .env.example .env
#
# ⚠️ NUNCA versionar .env ou ia.env com tokens/senhas reais.

# Porta do servidor (Express)
PORT=3000

# Segredo da sessão (express-session)
# Use uma string longa e aleatória (>= 32 chars).
SESSION_SECRET=coloque_um_segredo_longo_e_aleatorio_aqui

# ===============================
# Integração com IA (Hugging Face Router)
# ===============================
# Token do Hugging Face (NÃO use o OpenAI_API_KEY aqui)
HF_TOKEN=coloque_seu_token_huggingface_aqui

# Modelo (com provider) usado pelo Hugging Face Router (OpenAI-compatible)
# Exemplo do projeto:
HF_CHAT_MODEL=HuggingFaceTB/SmolLM3-3B:hf-inference

# Ambiente (opcional)
NODE_ENV=development
